{
  "name": "website-analyzer",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm run build"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.24.0"
  },
  "engines": { "node": ">=18.0.0" }
}
